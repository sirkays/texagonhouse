services:
  traefik:
    image: traefik:v3.1
    container_name: traefik
    command:
      - --providers.docker=true
      - --providers.docker.exposedbydefault=false
      - --entrypoints.web.address=:80
      - --entrypoints.websecure.address=:443
      - --certificatesresolvers.le.acme.httpchallenge=true
      - --certificatesresolvers.le.acme.httpchallenge.entrypoint=web
      - --certificatesresolvers.le.acme.email=${LETSENCRYPT_EMAIL}
      - --certificatesresolvers.le.acme.storage=/letsencrypt/acme.json
      - --log.level=INFO
      - --accesslog=true
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock:ro
      - ./letsencrypt/acme.json:/letsencrypt/acme.json
    labels:
      # Redirect all HTTP to HTTPS
      - "traefik.http.routers.http-catchall.rule=HostRegexp(`{host:.+}`)"
      - "traefik.http.routers.http-catchall.entrypoints=web"
      - "traefik.http.routers.http-catchall.middlewares=redirect-to-https"
      - "traefik.http.middlewares.redirect-to-https.redirectscheme.scheme=https"
    networks: [web]
    restart: unless-stopped

  app:
    build: .
    container_name: nextapp
    env_file: .env
    labels:
      - "traefik.enable=true"
      # Point Traefik at the *actual* network name (we force it to be 'web' below)
      - "traefik.docker.network=web"

      # Match either hostname (Traefik v3 syntax)
      - "traefik.http.routers.next.rule=Host(`texagonui.esm.name.ng`) || Host(`texagon.epichouse.online`)"
      - "traefik.http.routers.next.entrypoints=websecure"
      - "traefik.http.routers.next.tls=true"
      - "traefik.http.routers.next.tls.certresolver=le"
      - "traefik.http.routers.next.service=next"

      # (Optional) Hint a single cert with both names
      - "traefik.http.routers.next.tls.domains[0].main=texagonui.esm.name.ng"
      - "traefik.http.routers.next.tls.domains[0].sans=texagon.epichouse.online"

      # Internal app port
      - "traefik.http.services.next.loadbalancer.server.port=3000"
    networks: [web]
    depends_on: [traefik]
    restart: unless-stopped

networks:
  web:
    # Force the network to be literally named "web" (no compose prefix)
    name: web
    external: false
